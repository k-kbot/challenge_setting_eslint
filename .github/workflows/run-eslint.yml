name: run-eslint

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install modelues
        run: npm i
      - name: Run ESLint
        run: eslint . --fix
